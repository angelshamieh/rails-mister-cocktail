<h2> Add a new review <%= @cocktail.name %></h2>
<%= simple_form_for [@cocktail, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
